<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@drawable/model_dialog_bg"
    android:orientation="vertical"
    android:padding="30.0pt">

    <TextView
        android:id="@+id/tvSrcDefault"
        android:layout_width="300.0pt"
        android:layout_height="40.0pt"
        android:background="@drawable/splash_focus"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center"
        android:text="设为默认解析"
        android:textColor="@android:color/black"
        android:textSize="18.0pt" />

    <TextView
        android:id="@+id/tvSrcDel"
        android:layout_width="300.0pt"
        android:layout_height="40.0pt"
        android:layout_marginTop="30.0pt"
        android:background="@drawable/splash_focus"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:gravity="center"
        android:text="删除解析"
        android:textColor="@android:color/black"
        android:textSize="18.0pt" />
</LinearLayout>